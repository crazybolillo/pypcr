[project]
name = "pypcr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.2",
    "psycopg2>=2.9.10",
]

[project.optional-dependencies]
production = [
    "gunicorn>=23.0.0",
]
